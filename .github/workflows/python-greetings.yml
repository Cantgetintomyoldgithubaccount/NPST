name: Delivery Pipeline
on:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:

install-pip-deps:
  runs-on: self-hosted
      steps:
        - name: Check out repository
          uses: actions/checkout@v3.5.2
